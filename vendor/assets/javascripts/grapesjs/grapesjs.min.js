/*! 
 * GrapesJS v0.20.4
 * This is a placeholder file. In production, you should either:
 * 1. Download the actual GrapesJS files from https://grapesjs.com/
 * 2. Use a CDN link in your layout
 * 3. Use npm/yarn to install grapesjs package
 * 
 * CDN Example:
 * <script src="https://unpkg.com/grapesjs"></script>
 */

console.warn('GrapesJS placeholder file loaded. Please replace with actual GrapesJS library.');

// Minimal stub to prevent errors during development
if (typeof window !== 'undefined' && !window.grapesjs) {
  window.grapesjs = {
    init: function(config) {
      console.error('GrapesJS not loaded. Please include the actual GrapesJS library.');
      return {
        getHtml: () => '',
        getCss: () => '',
        setComponents: () => {},
        setStyle: () => {},
        getContainer: () => document.createElement('div'),
        on: () => {},
        I18n: {
          addMessages: () => {},
          setLocale: () => {}
        },
        Canvas: {
          getFrameEl: () => null
        },
        BlockManager: {
          add: () => {}
        }
      };
    }
  };
}