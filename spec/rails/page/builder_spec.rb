# frozen_string_literal: true

RSpec.describe Rails::Page::Builder do
  it "has a version number" do
    expect(Rails::Page::Builder::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
